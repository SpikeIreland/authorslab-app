import { PlaceholderTab } from '../_components/PlaceholderTab'

export default async function ScriptTabPage() {
  return (
    <PlaceholderTab
      status="coming-soon"
      title="Script"
      description="Adapt your finished book into a screenplay or teleplay. Lives as a tab inside the parent project so the source material is always close. Aspirational — opens once a project has been launched."
    />
  )
}
