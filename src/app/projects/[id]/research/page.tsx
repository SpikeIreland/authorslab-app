import { PlaceholderTab } from '../_components/PlaceholderTab'

export default async function ResearchTabPage() {
  return (
    <PlaceholderTab
      status="coming-soon"
      title="Research"
      description="The same creative companion you meet on Home, but with this project's manuscript and characters loaded as context. Ask anything — research a setting, develop a character, talk through a tricky scene. One backend, two surfaces."
    />
  )
}
