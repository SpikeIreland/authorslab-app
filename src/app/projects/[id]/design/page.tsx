import { PlaceholderTab } from '../_components/PlaceholderTab'

export default async function DesignTabPage() {
  return (
    <PlaceholderTab
      status="coming-soon"
      title="Design"
      description="Cover, front matter, back matter, interior format. The creative-visual work that turns a manuscript into a book. Taylor takes you through it. We're splitting this from the existing Publishing Hub — coming next."
    />
  )
}
