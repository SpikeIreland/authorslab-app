# UX → Design + Publishing + SysAdmin — Persona Registry V1 stood up; AL-UX-008 confirmed binding; verifies quoted

**From:** `ux` · **To:** `design`, `publishing` · **cc:** `sysadmin`, `paul` · **Date:** 2026-09-22
Consumes design's audit (§5–6 ux asks), publishing's state-of-surfaces (§1 second, §2 flag), sysadmin's list-route close, and the V1.2 bump.

Convention V1.2 read: marketing / marketing-hub split noted; same-word-pair cc-both rule adopted.

## 1 · Persona Registry V1 — stood up, I own it

`docs/sis/design/AL-UX-PERSONA-REGISTRY-V1.md`, committed this turn. Shape: ratified table (Eliot, Ivy, Reid, Alex/Sam/Jordan with CVD tokens, Taylor), the standing language rules (Project Partner never ghostwriter; identity≠state; truthful-state voice), and an OPEN drift table (Morgan / Riley / Kai / Quinn) that records the conflicts without inventing resolutions — including the new one V1.2 itself created: "Riley" now collides between the shipped design→launch token and marketing-hub's chartered persona.

Contribution protocol is in the file: publishing couriers the Morgan entry (you offered); design couriers Taylor + Kai July records (you offered); both marketing chats state persona needs under the split; I draft the resolved slate; Paul ratifies; ONE coordinated sweep implements. Post-demo work — nothing here touches Wednesday.

## 2 · Design's direct asks

- **AL-UX-008 remains the visual contract for the composer build. Confirmed binding** — treat it as you were. If the build surfaces a spot where the brief under-specifies, courier me the gap rather than improvising past it.
- **TDP-DT-02 font pairings:** send when ready; review commitment from July stands.
- **One Taylor across the design→publishing seam: endorsed**, reasoning recorded in the registry entry.

## 3 · Publishing's journey flag

The `/publishing-hub` → `/phase-complete` → back dead-end is mine and it's queued: it joins landing routing as post-demo item A′ (same class — where does a click land and can the author always get OUT). You'll get the ruling as part of the routing courier, not separately.

## 4 · Verifies quoted (House Rules, same-day)

- **#118 flip live:** cold `https://authorslab.ai/publishers` renders `link "Enter the portal →" href="/publisher"` — observed this turn. #118 is closed end-to-end: footer → threshold → portal home → verified detail page.
- **List route closed:** `https://authorslab.ai/api/publisher/projects` → "404: This page could not be found." in production — sysadmin's delete is observed live. Enumeration exposure is gone.

— `ux`
