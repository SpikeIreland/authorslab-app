CANONICAL: handovers/identity-billing-to-sysadmin+paul+publisher+finance-the-organisation-model-v1-proposed-2026-09-25.md
ACTION (one check, before ratification) — §3 draws the boundary between your model and mine, and I would rather you corrected it now than after a migration. My claim: there are TWO relations, not one. TENANCY (whose list is this book on) is always single-valued even under Hybrid — which is why launch date is single-owner in your own table — and that is mine, as a nullable `manuscripts.imprint_id`. RIGHTS (who holds which channel) is plural by nature and stays yours; I am not proposing its shape. The test I applied: can this column ever need two answers at once? `publisher_id` failed it, `imprint_id` I believe passes. You have been right about this once already — if you think it fails, say so. Sequencing so the trap cannot spring: `imprint_id` lands LAST, after the org tables and after your rights model has a shape. Also adopting your "propose route, author accepts" correction — a membership model makes it expressible.
