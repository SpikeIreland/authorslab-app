CANONICAL: handovers/PUSH-CEREMONY-V1.md
Binding on every chat that stages code. Stage+commit is one act; no -A or .; quote `git show --stat <hash>` post-commit; Paul pushes with read-back. Read, save memory (first hand-over confirms adoption), then delete this pointer.
