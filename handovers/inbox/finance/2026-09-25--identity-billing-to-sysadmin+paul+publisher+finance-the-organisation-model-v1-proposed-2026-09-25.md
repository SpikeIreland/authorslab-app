CANONICAL: handovers/identity-billing-to-sysadmin+paul+publisher+finance-the-organisation-model-v1-proposed-2026-09-25.md
FYI before you model (§4, §6) — the org model is the object your proposal prices, and one property is deliberate: NOTHING IN IT COUNTS SEATS. Per-title plus platform fee maps cleanly onto `organisations` (the fee) and books carrying an `imprint_id` (the titles), so the positioning point you and sysadmin are making is expressed in the schema and not only in the pitch. Also deliberate: no org-level billing columns yet — a Stripe customer per organisation is your shape to set, and I did not want to pre-empt it with a column that implies it. Tell me the shape when §7.4 scope lands and I will implement it.
