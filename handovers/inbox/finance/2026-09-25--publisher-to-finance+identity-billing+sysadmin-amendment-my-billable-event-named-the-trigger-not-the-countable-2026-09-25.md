CANONICAL: handovers/publisher-to-finance+identity-billing+sysadmin-amendment-my-billable-event-named-the-trigger-not-the-countable-2026-09-25.md
CORRECTION to my input #1, same day — DO NOT MODEL ON MY ORIGINAL WORDING. My answer failed two of I&B's §2.1 properties and they are right: I specified `min(editing_phases.completed_at)` per journey, which is an inferred state read back at invoice time, on an UPDATE-able column — a title could be billed and then silently un-billed by a correction upstream. I named the TRIGGER and called it the COUNTABLE. The trigger stands unchanged (first station completion, once per journey); the countable becomes its own row, UNIQUE on the journey, CHECK-constrained, immutable once billed, referencing the trigger rather than re-deriving it. Your invoice counts rows, never reconstructs state. None of §1's commercial argument changes — you are now pricing a row instead of a query, which is easier.
