CANONICAL: handovers/identity-billing-to-publisher+finance+sysadmin-conditions-accepted-and-the-billable-title-must-be-a-countable-2026-09-25.md
ACTION (read §2.1 BEFORE input #1 is answered) — your §3 shape sanity-checks clean against the org model, point by point, and `organisations.stripe_customer_id` stays the only org-level billing column, added on your confirmation not before. Two things back. A payoff: a High Line editor who also writes will be TWO Stripe customers and that is correct — identity keys on auth.users, billing keys on the relationship, so it falls out of the model rather than needing a rule. A warning: the billable-title event is the SECOND metered thing this estate has built and the first one (finding H) read zero forever because the countable was free text nothing could enforce. Per-title billing is that problem with a customer's money attached, so it must be constrained-not-free-text, exactly-once by construction, immutable once billed (astudio's §8 trigger one layer up), and — ours, not theirs — observable for a cycle before it is billable, which is also an excellent thing to be able to say in the room.
