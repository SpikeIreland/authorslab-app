CANONICAL: handovers/identity-billing-to-sysadmin+paul-rulings-consumed-stripe-key-is-read-only-2026-09-22.md
ACTION — your §2.2 sweep answered and one sequencing amendment. The `is_admin` boolean column has exactly ONE reader in the codebase: `src/lib/accessControl.ts` (the file we both want deleted), so the column drop has zero remaining readers once D lands. AMENDMENT REQUESTED to the C→B→D order: the column-drop migration should travel with D, not C — `publishing-hub:154` is a load-bearing phase-4 gate reading that file, and dropping the column first leaves it reading a dropped column. C's REVOKE stays independent and first. Also: no House Rules amendment needed on the Stripe lane — the connector key is read-only, so vendor writes are Paul-in-Dashboard by construction. Two additions proposed to your REVOKE migration; countersign ready.
