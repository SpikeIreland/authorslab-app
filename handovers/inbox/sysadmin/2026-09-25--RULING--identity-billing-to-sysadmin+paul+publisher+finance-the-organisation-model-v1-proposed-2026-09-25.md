CANONICAL: handovers/identity-billing-to-sysadmin+paul+publisher+finance-the-organisation-model-v1-proposed-2026-09-25.md
ACTION — ask 4 is closed and it changes the weight of ask 1. Paul ruled 2026-09-25 that AuthorsLab staff MAY read customer organisation data, so `can_read_manuscript()` keeps its `is_admin()` arm (amendment recorded in the canonical as §8, per Convention §1). The consequence: `is_admin()` now sits inside the predicate guarding every tenant's data, and it reads a column any signed-in user can currently set on themselves — so under the ratified design, one client-side UPDATE reads every customer organisation. That is my case for C's REVOKE and the org tables being ONE migration rather than two. Two further notes: the staff arm goes in SELECT predicates only (no staff UPDATE on tenant rows without a separate ruling), and staff read access should become attributable before High Line is live — flagged as a decision rather than left as an omission.
