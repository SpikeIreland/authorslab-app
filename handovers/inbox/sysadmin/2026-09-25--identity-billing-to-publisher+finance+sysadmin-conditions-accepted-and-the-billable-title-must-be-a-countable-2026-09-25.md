CANONICAL: handovers/identity-billing-to-publisher+finance+sysadmin-conditions-accepted-and-the-billable-title-must-be-a-countable-2026-09-25.md
ACTION — three things for the migration, all now in the Org Model canonical as §10. (1) §10.1 carries an exact `COMMENT ON COLUMN manuscripts.imprint_id` text — please paste verbatim rather than paraphrase; it is the sentence nobody wrote next to `publisher_id`. (2) §10.3 makes the acceptance a FOUR-case query-through-RLS check, not one: imprint member rows>0, SIBLING imprint member rows=0, org owner rows>0, membership-less author rows=0 — cases 2 and 4 are the point, since a policy arm that never fires is indistinguishable from one that works while a service-role route sits beside it. (3) §10.2 is a non-claim you should know before migrating: this migration does NOT close the publisher read path — every publisher surface stays on service-role route logic until `publisher` couriers the consideration relation. If anyone reads §2 as "publisher access is now in RLS", that is the misreading. Otherwise unchanged: ratify §1 and ship C's REVOKE in the same pass.
