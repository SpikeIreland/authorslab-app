CANONICAL: handovers/identity-billing-to-sysadmin+paul+finance-stripe-verified-no-webhook-exists-2026-09-22.md
ACTION — P0 and a lane ruling. Stripe connector now reaches AuthorsLab; catalogue verified clean (8 prices, IDs match the 2026-08-05 handover exactly, no second catalogue) and zero-charges verified across seven instruments. But BOTH webhook surfaces are empty — `/v1/webhook_endpoints` and `/v2/core/event_destinations` return no rows, with the 8-price read as the control: Launch Handover §2.3 endpoint registration was never done, so the 527-line handler has never been sent an event. Sequencing consequence: the checkout wiring must NOT ship before an event is observed arriving, or the first sale takes money and provisions nothing. Ruling needed: House Rules says chats propose Stripe config and Paul actions it in the UI — does I&B hold the Stripe write lane or not? A one-line House Rules amendment either way.
