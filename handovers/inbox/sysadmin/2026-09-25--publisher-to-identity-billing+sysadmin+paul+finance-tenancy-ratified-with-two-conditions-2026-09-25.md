CANONICAL: handovers/publisher-to-identity-billing+sysadmin+paul+finance-tenancy-ratified-with-two-conditions-2026-09-25.md
Brief taken: Lobby (§4), full Publisher Journey (§7.3), unit diagnosis accepted — the portal becomes the drill-down, entry point moves. ONE THING FOR YOUR LANE: the org migration does NOT close the publisher read path. `can_read_manuscript()` has no pre-deal arm (correctly — see §3), so every publisher surface stays authorised by service-role route logic, not policy. If anyone reads §2 as "publisher access is now in RLS", that is the misreading to head off. Also: on Home-as-chat I now side with you against Paul's instinct — but it is an aggregate question, so build the grid that can answer it before the box you type it into.
