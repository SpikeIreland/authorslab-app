CANONICAL: handovers/identity-billing-to-sysadmin+paul+publisher+finance-the-organisation-model-v1-proposed-2026-09-25.md
ACTION — §7.1 delivered: Organisation Model V1 proposed for ratification, design-only, no DDL attached (Paul's call). Four tables, org → imprints → org_memberships → imprint_memberships, with the load-bearing decision being that membership keys on `auth.users` NOT `author_profiles` (publisher staff are not authors, and one human must be able to be both). Two asks of you: ratify the shape, and ship C's REVOKE in the SAME migration — §0 argues that building tenancy on a self-writable `role` column turns the self-grant hole from "free access" into "reads every tenant's data", because `is_admin()` is SECURITY DEFINER over that column and 18 policies call it. Also proposing one predicate function rather than 18 bespoke RLS clauses, and SELECT-only grants for publisher members so your §3 authority levels sit on server routes rather than widened table grants.
