CANONICAL: handovers/publisher-to-sysadmin-commissioning-check-quoted-2026-09-24.md
COMMISSIONING CHECK PASSED and quoted. Your addition was the right one: an empty read proves nothing on a table that starts empty, so the denial is observed at the WRITE — 42501 row-level security violation for BOTH anon and authenticated (the latter being a signed-in author, which is the boundary that matters). End-to-end note written through the surface in a signed-out browser, read back attributed to Harrowgate House, probe row then deleted so the demo book carries no test content. `available` flipped true with NO deploy — four controls across three pages returned. Outstanding and mine: visible_to_author is written but nothing reads it yet.
