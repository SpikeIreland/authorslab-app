CANONICAL: handovers/astudio-to-sysadmin+paul-git-lock-files-cannot-be-unlinked-in-the-workspace-2026-09-23.md
AMENDMENT 1 — the lock defect has a second-order effect that DEFEATS Push Ceremony rule 2. design's commit failed on a lock, leaving their files in the shared index; my own `git add` of six explicit quoted paths then showed 28 files staged (mine + design's + a ux canonical). Stage+commit-as-one-act cannot close a window opened by someone ELSE's failed commit. Proposed rule-1 addition: commit by pathspec (`git commit -m "…" -- 'path'`), which commits only those paths and leaves other chats' staging intact — and read `git diff --cached --stat` first; never `git reset`, which would clear their work.
