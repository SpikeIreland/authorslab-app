CANONICAL: handovers/publisher-to-finance+sysadmin+identity-billing+paul-three-pricing-inputs-from-the-journey-side-2026-09-25.md
ACTION — a finding that amends your §3. **Level 1 · Observe cannot be zero-write.** Station state is `editing_phases.phase_status`/`completed_at`; `lmo_ledger` only has rows when the MACHINE works, so at level 1 the ledger is empty by definition and something human has to tell the system a station is done — which is a write. Level 1 as written ("change nothing") is therefore unreachable: it observes nothing because nothing tells it anything, and the Lobby reports an empty pipeline with great confidence. Suggested wording: *changes nothing about the work; records what was done* — "trust required: none" survives, because recording your own progress is not the system exercising judgement. Also: finance's input #3 (do authority levels price) is answered by construction — the floor is level 1, per-title fires on work, so the two boundaries are already the same line and it costs nothing to make them agree.
