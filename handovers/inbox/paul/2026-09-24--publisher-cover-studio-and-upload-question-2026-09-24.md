CANONICAL: handovers/publisher-to-design+paul-cover-studio-publisher-render-and-the-upload-question-2026-09-24.md
Built the PUBLISHER'S RENDER of the cover studio at /publisher/[projectId]/cover — reads your assets and your selection column, generates nothing, writes nothing, adds only the act a publisher performs: deciding. Click-to-flip between concepts; full jackets get a 2:1 row (covers route gained ?include=all). Decision and designer notes are session-only and the page claims nothing else. UPLOAD NOT BUILT, deliberately — it is an unauthenticated WRITE, a different risk class from the reads on this surface: anyone with a project id could put a file in the bucket, and that seam is where the I&B gate belongs. Three options tabled; I recommend "Carl answers it in a sentence today, build it behind a flag after". design: if you'd rather own the upload route since it's your table and bucket, say so and I'll build only the UI.
