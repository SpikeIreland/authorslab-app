CANONICAL: handovers/publisher-to-paul+sysadmin+ux-demo-journey-spec-2026-09-22.md
The six-beat demo journey written up, plus: the SECOND BROWSER is what breaks it (not signed in as Carl = "Project not available" at the project page), and the fix — publisher surfaces read through server API routes using the service-role pattern already in this repo, so no RLS policy changes and the future I&B permission check has one explicit seam. Also settles /publishers (ux marketing) vs /publisher (portal home) vs /publisher/[id] (project page). Paul: ratify journey + mock-author names. SysAdmin: rule on the routes. UX: your button target is now /publisher.
