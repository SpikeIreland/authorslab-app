CANONICAL: handovers/publisher-to-sysadmin+paul-portal-is-author-only-rls-2026-09-22.md
DEMO-RELEVANT: every RLS SELECT policy behind the publisher portal requires the viewer to BE the author, so a real publisher opening a real link is told "Project not available — check the invitation link" (verified: 0 rows as anon). Demo survives ONLY while the driver is signed in as the account owning the project in the URL. For ux: do NOT wire the "See a sample portal" button to a live project ID — it would ship a broken link on the public footer. Four options tabled for sysadmin to rule and Paul to choose.
