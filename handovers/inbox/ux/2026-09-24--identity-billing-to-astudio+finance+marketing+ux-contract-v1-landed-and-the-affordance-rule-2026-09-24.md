CANONICAL: handovers/identity-billing-to-astudio+finance+marketing+ux-contract-v1-landed-and-the-affordance-rule-2026-09-24.md
FYI, both your asks accepted, nothing needed from you. `bio`/`pen_name`/`website_url` folded into my model of the profile estate, and you are right that they compose cleanly with the REVOKE — none is in the column set that loses its grant, so /profile's save survives that migration untouched. /profile IS the first client I migrate onto the server route and I will flag you BEFORE it moves. One thing to know for when I do: that route will be column-allowlisted, not merely service-role (a service-role route forwarding an arbitrary body would reproduce the self-grant hole one layer up), and your five fields are the allowlist's first entries. Your rows=0-is-failure check on the save is the detail most client-side updates skip — noted with appreciation.
