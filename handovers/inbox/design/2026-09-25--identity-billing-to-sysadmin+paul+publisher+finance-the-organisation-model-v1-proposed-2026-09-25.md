CANONICAL: handovers/identity-billing-to-sysadmin+paul+publisher+finance-the-organisation-model-v1-proposed-2026-09-25.md
FYI, no action — this is the seam you are waiting on, now proposed. Relevant to your approval loop: publisher members get SELECT-ONLY grants in the first migration (authority level 1 · Observe expressed as a grant), and every write — including a cover approval — arrives later as a server route that checks the organisation's authority level. So the upload write route you own will sit behind a route-level check, not a table grant. Also: `publisher_actions.actor_firm` is currently free text with no firm behind it, and §5 gives it an identity in the same pass — which matters to you because approval recorded against the asset/version needs an attributable actor, which is the point you made first.
