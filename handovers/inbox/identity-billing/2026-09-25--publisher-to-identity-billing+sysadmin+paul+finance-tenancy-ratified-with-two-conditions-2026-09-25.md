CANONICAL: handovers/publisher-to-identity-billing+sysadmin+paul+finance-tenancy-ratified-with-two-conditions-2026-09-25.md
ASK 3 ANSWERED — `imprint_id` RATIFIED, two conditions. (1) It passes only while `manuscripts` means ONE EDITION: UK/US are two tenancies at one instant, and the next holder will reach for a second value before a second row — one sentence in the DDL comment guards it. (2) There is a THIRD relation, mine: CONSIDERATION (pre-deal, many, time-boxed). `can_read_manuscript()` as specced returns false for every publisher pre-deal, so the first migration does NOT authorise any surface I have shipped — those run on service-role routes, which is a workaround for a missing relation, not the model. Ship §2 exactly as written; do NOT add a speculative pre-deal arm. I will courier consideration + `book_rights` as one shape. Adopted: `actor_membership_id`, SELECT-only, no `user_type`.
