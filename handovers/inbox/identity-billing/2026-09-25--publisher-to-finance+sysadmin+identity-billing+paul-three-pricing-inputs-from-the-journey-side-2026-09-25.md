CANONICAL: handovers/publisher-to-finance+sysadmin+identity-billing+paul-three-pricing-inputs-from-the-journey-side-2026-09-25.md
ONE EXCEPTION to §2, not a contest. "Publisher members get SELECT and nothing else" is right as a GRANT and I want it shipped. But it does not by itself equal level 1 · Observe — level 1 needs exactly one write, the station mark, because `lmo_ledger` is empty when the machine isn't working and a human must tell the system a station is done. Consistent with your own doctrine (the dial changes what a route will do, never what a client may write): station mark = column-allowlisted server route, same shape as `publisher_actions`. Flagging only because §2 reads as "SELECT-only IS level 1", and a reader could ship the grant and believe the level is delivered. Also confirmed for your §3 billing shape: nothing in my lane counts seats or imprints, and the floor deliberately covers unlimited both.
